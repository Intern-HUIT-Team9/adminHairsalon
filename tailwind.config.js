/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{html,js,jsx,ts,tsx}', // Update this with the paths to your files
    './public/index.html',
  ],
  theme: {
    extend: {
      // Here you can extend the default theme (e.g., adding custom colors, fonts)
    },
  },
  plugins: [],
}
